name: Build Aether Runtime Center APK

on:
  pull_request:
    paths:
      - 'projects/aether-runtime-center/**'
      - '.github/workflows/aether-runtime-center-build.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        working-directory: projects/aether-runtime-center

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle 8.9
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK packages
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager 'platforms;android-35' 'build-tools;35.0.0' 'platform-tools'

      - name: Build debug APK from independent source
        run: gradle --no-daemon --stacktrace :app:assembleDebug

      - name: Verify independent package and launcher
        run: |
          APK=app/build/outputs/apk/debug/app-debug.apk
          AAPT="$ANDROID_HOME/build-tools/35.0.0/aapt"
          test -f "$APK"
          test -x "$AAPT"
          "$AAPT" dump badging "$APK" | tee apk-badging.txt
          PACKAGE_LINE=$(grep "^package:" apk-badging.txt)
          LAUNCHER_LINE=$(grep "^launchable-activity:" apk-badging.txt)
          echo "$PACKAGE_LINE"
          echo "$LAUNCHER_LINE"
          echo "$PACKAGE_LINE" | grep -F "name='company.taowind.aethercenter'"
          echo "$LAUNCHER_LINE" | grep -F "name='company.taowind.aethercenter.MainActivity'"
          unzip -p "$APK" classes.dex > classes.dex
          if grep -aFq 'company/taowind/aethermobile/bootstrap/MainActivity' classes.dex; then
            echo 'Aether Mobile launcher leaked into Runtime Center APK' >&2
            exit 1
          fi

      - name: Upload APK artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Aether-Runtime-Center-v0.1.1-independent
          path: projects/aether-runtime-center/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
